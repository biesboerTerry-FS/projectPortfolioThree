{
  "name": "spotifyAtMidnight",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend && (cd frontend && VITE_BACKEND_URL= npm run build)"
  }
}
